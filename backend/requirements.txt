fastapi>=0.110
uvicorn[standard]>=0.29
sqlmodel>=0.0.16
pydantic-settings>=2.2
python-jose[cryptography]>=3.3
httpx>=0.27
python-multipart>=0.0.9
# Optional — only needed for the Google Sheets ingest (imported lazily):
#   google-api-python-client>=2.0
#   google-auth>=2.0
# Optional — only needed for Stripe billing (imported lazily):
#   stripe>=9.0
# Optional — only needed to ingest a local .xlsx for testing:
#   openpyxl>=3.1
